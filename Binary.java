import static java.lang.Math.*;

public class Binary {
    public void zmien(int liczba){
        int length;
        length = (int) (Math.log10(liczba) + 1); // ilosc cyfr liczby
        System.out.println(length);
        //co teraz?
    }

}
